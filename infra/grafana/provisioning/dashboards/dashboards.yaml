apiVersion: 1

providers:
  - name: ticketing-cinema
    folder: Ticketing Cinema
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/dashboards
